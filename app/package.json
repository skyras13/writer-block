{
  "name": "book-factory-app",
  "version": "1.0.0",
  "private": true,
  "description": "Local control panel for the Book Factory — dashboard, pipeline tracker, checkpoint approvals, manuscript reader/editor, and KDP exporter. File-based, no database.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "doctor": "node export/build.js --doctor"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
